import React from 'react';
import Layout from './Hoc(Hight order component)/Layout';

const Routes = (props) => {
  return (
    <Layout>
      Second line
    </Layout>
  )
}

export default Routes;
